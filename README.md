# The Able Few Tility

## Install

 * [Node.js](http://nodejs.org/)
 * `gem install bundler`
 * `bundle install`
 * `npm install`
 * `cd app/assets/stylesheets`
 * `bourbon install`
 * `neat install`
 * `grunt run` or `npm start`